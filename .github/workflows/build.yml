name: Build Matisse3G Kernel

on:
  workflow_dispatch:
  push:
    branches: [pmos-matisse3g-fixes]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          ref: pmos-matisse3g-fixes
          submodules: false

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-linux-gnueabihf bc bison flex libssl-dev make cpio python3

      - name: Verify no CONFIG_RT_GROUP_SCHED
        run: |
          grep -i "RT_GROUP_SCHED" arch/arm/configs/matisse3g_defconfig && \
            echo "WARNING: RT_GROUP_SCHED found in defconfig!" || \
            echo "OK: RT_GROUP_SCHED not in defconfig"

      - name: Configure kernel
        run: |
          export ARCH=arm
          export CROSS_COMPILE=arm-linux-gnueabihf-
          make matisse3g_defconfig
          echo "--- .config RT settings ---"
          grep -i "RT_GROUP_SCHED\|RT_RUNTIME\|PREEMPT" .config || true

      - name: Build kernel
        run: |
          export ARCH=arm
          export CROSS_COMPILE=arm-linux-gnueabihf-
          make -j$(nproc) zImage 2>&1 | tee /tmp/build.log ; test ${PIPESTATUS[0]} -eq 0

      - name: Build Image target
        run: |
          export ARCH=arm
          export CROSS_COMPILE=arm-linux-gnueabihf-
          make -j$(nproc) Image 2>&1 | tee /tmp/build2.log ; test ${PIPESTATUS[0]} -eq 0

      - name: List built images
        run: |
          echo "=== arch/arm/boot/ ==="
          ls -la arch/arm/boot/ 2>/dev/null
          echo ""
          echo "=== compressed/ ==="
          ls -la arch/arm/boot/compressed/ 2>/dev/null
          echo ""
          echo "=== vmlinux ==="
          ls -la vmlinux 2>/dev/null
          echo ""
          echo "=== Top build errors ==="
          grep -i 'error:' /tmp/build.log 2>/dev/null | head -10 || echo "no errors found in log"

      - name: Create boot.img
        run: |
          if [ -f arch/arm/boot/Image ]; then
            KERNEL=arch/arm/boot/Image
            echo "Using Image: $(wc -c < $KERNEL) bytes"
          elif [ -f arch/arm/boot/zImage ]; then
            KERNEL=arch/arm/boot/zImage
            echo "Using zImage: $(wc -c < $KERNEL) bytes"
          else
            echo "ERROR: No kernel image found!"
            echo "Build log errors:"
            grep -i 'error\|fatal\|cannot\|fail' /tmp/build.log 2>/dev/null | head -20
            exit 1
          fi

          python3 << 'PYEOF2'
          import struct, os, urllib.request, base64, json

          if os.path.exists('arch/arm/boot/Image'):
              kernel_path = 'arch/arm/boot/Image'
          elif os.path.exists('arch/arm/boot/zImage'):
              kernel_path = 'arch/arm/boot/zImage'
          else:
              raise FileNotFoundError('No kernel image found')

          with open(kernel_path, 'rb') as f:
              kernel = f.read()
          print(f'Kernel: {len(kernel)} bytes from {kernel_path}')

          token = os.environ['GH_TOKEN']
          repo_url = os.environ.get('GITHUB_REPOSITORY', 'criollojoel10/android_kernel_samsung_msm8226')
          url = f'https://api.github.com/repos/{repo_url}/contents/boot/ramdisk.gz'
          req = urllib.request.Request(url, headers={'Authorization': f'token {token}'})
          resp = urllib.request.urlopen(req)
          data = json.loads(resp.read())
          ramdisk = base64.b64decode(data['content'])
          print(f'Ramdisk: {len(ramdisk)} bytes')

          page_size = 2048
          base_addr = 0x00000000
          kernel_offset = 0x00008000
          ramdisk_offset = 0x02000000
          tags_offset = 0x01e00000
          cmdline = b'androidboot.hardware=qcom user_debug=23 msm_rtb.filter=0x37 zcache.enabled=1 zcache.compressor=lz4 buildvariant=userdebug'

          def pad(value):
              return (value + page_size - 1) & ~(page_size - 1)

          kernel_padded = pad(len(kernel))
          ramdisk_padded = pad(len(ramdisk))

          header = bytearray(page_size)
          header[0:8] = b'ANDROID!'
          struct.pack_into('<I', header, 8, len(kernel))
          struct.pack_into('<I', header, 12, base_addr + kernel_offset)
          struct.pack_into('<I', header, 16, len(ramdisk))
          struct.pack_into('<I', header, 20, base_addr + ramdisk_offset)
          struct.pack_into('<I', header, 24, 0)
          struct.pack_into('<I', header, 28, 0)
          struct.pack_into('<I', header, 32, base_addr + tags_offset)
          struct.pack_into('<I', header, 36, page_size)
          struct.pack_into('<I', header, 40, 0)
          struct.pack_into('<I', header, 44, 0)
          header[48:64] = b'SM-T531\x00'
          header[64:64+len(cmdline)] = cmdline

          with open('boot.img', 'wb') as out:
              out.write(header)
              out.write(kernel)
              out.write(b'\x00' * (kernel_padded - len(kernel)))
              out.write(ramdisk)
              out.write(b'\x00' * (ramdisk_padded - len(ramdisk)))

          total = page_size + kernel_padded + ramdisk_padded
          print(f'boot.img: {total} bytes ({total // 1024} KB)')
          PYEOF2

          ls -la boot.img

      - name: Upload boot.img artifact
        uses: actions/upload-artifact@v4
        with:
          name: matisse3g-boot-${{ github.sha }}
          path: boot.img
          retention-days: 30

      - name: Upload kernel Image artifact
        uses: actions/upload-artifact@v4
        with:
          name: matisse3g-kernel-${{ github.sha }}
          path: arch/arm/boot/
          retention-days: 30

      - name: Build summary
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Config**: matisse3g_defconfig" >> $GITHUB_STEP_SUMMARY
          echo "- **CONFIG_RT_GROUP_SCHED**: NOT SET (fixed!)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Flash instructions" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "adb push boot.img /sdcard/" >> $GITHUB_STEP_SUMMARY
          echo "adb shell su -c 'dd if=/sdcard/boot.img of=/dev/block/platform/msm_sdcc.1/by-name/boot'" >> $GITHUB_STEP_SUMMARY
          echo "adb reboot" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
